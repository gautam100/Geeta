# Task Management App with Lifecycle Methods

## Aim
To add lifecycle methods to the task management app to manage component behavior.

This project uses a class component and lifecycle methods:
- `componentDidMount()`
- `componentDidUpdate()`
- `componentWillUnmount()`

## Run
```
npm install
npm run dev
```
